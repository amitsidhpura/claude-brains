<!-- The panel card's SVG_X (chat.html) verbatim, stroked in the Reject button's text colour
     (the fg token, #d5d8dd); the SVG loader has no currentColor context. Keep path in sync.
     NB: a literal double hyphen is illegal in XML comments, so no CSS var names here. -->
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#d5d8dd" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
