<!-- The panel card's SVG_CHECKS (chat.html, lucide check-check) verbatim, stroked in the
     button's text white; the SVG loader has no currentColor context. Keep path in sync.
     NB: a literal double hyphen is illegal in XML comments, so no CSS var names here. -->
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 7 17l-5-5"/><path d="m22 10-7.5 7.5L13 16"/></svg>
