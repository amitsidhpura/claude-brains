<!-- The panel card's SVG_CHECK (chat.html) verbatim, stroked in the Accept button's text
     white; the SVG loader has no currentColor context. Keep path in sync with chat.html.
     NB: a literal double hyphen is illegal in XML comments, so no CSS var names here. -->
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
